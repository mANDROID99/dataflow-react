import { GraphTemplate } from '@react-ngraph/core';
import graph from './basicMultiLine.json';

export const BASIC_MULTILINE_TEMPLATE: GraphTemplate = {
    id: 'basicMultiLine',
    label: 'Basic Multi-Line',
    graph
}
