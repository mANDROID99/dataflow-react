import { GraphTemplate } from '@react-ngraph/core';
import graph from './apiDataConditional.json';

export const API_DATA_CONDITIONAL_TEMPLATE: GraphTemplate = {
    id: 'api-data-conditional',
    label: 'Api Data (Conditional)',
    graph
}
